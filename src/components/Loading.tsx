const Loading = () => {
  return (
    <div className="bg-background flex flex-1 h-screen items-center justify-center">
      <p className="text-primary text-3xl animate-pulse">Loading...</p>
    </div>
  );
};

export default Loading;
